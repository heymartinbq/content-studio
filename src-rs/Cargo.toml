[package]
name = "src-rs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# No dependencies to ensure 0 errors/warnings on all environments
# avoiding proc-macro expansion issues in restricted IDEs

[profile.release]
opt-level = "s"
lto = true
